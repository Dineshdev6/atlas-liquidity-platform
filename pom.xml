<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!--
      LAYER 1 TEACHING NOTE

      This is an "aggregator + parent" POM. It does two jobs at once:

        1. Aggregator  - <modules> tells Maven which projects build together,
                         and in what dependency order. This is the "reactor".
        2. Parent      - child modules inherit <properties>, <dependencyManagement>
                         and <pluginManagement> from here.

      Note what we deliberately did NOT do: we did not set
      spring-boot-starter-parent as our <parent>. Most tutorials do. Large
      enterprises usually do not, because you only get one parent in Maven and
      most organisations want that slot for their own corporate parent POM
      (build rules, artifact repo config, licence checks, security scanning).

      Instead we IMPORT the Spring Boot BOM in <dependencyManagement> below.
      Same version alignment, no parent slot consumed.
      Be ready to explain this distinction - it is a common Java interview probe.
    -->

    <groupId>com.atlas.liquidity</groupId>
    <artifactId>atlas-liquidity-platform</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>Atlas Liquidity Platform</name>
    <description>Cash and intraday liquidity management platform</description>

    <modules>
        <module>liquidity-common</module>
        <module>reference-data-service</module>
        <module>position-service</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.release>${java.version}</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <spring-boot.version>3.5.6</spring-boot.version>
        <jacoco.version>0.8.12</jacoco.version>
        <surefire.version>3.5.2</surefire.version>
            <maven.compiler.parameters>true</maven.compiler.parameters>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!--
              Importing a BOM: scope=import + type=pom pulls in every version
              declaration from spring-boot-dependencies without inheriting from it.
              After this, child modules declare Spring dependencies with NO <version>.
            -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Our own modules. Same idea: children declare them without a version. -->
            <dependency>
                <groupId>com.atlas.liquidity</groupId>
                <artifactId>liquidity-common</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${surefire.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <!--
              JaCoCo measures test coverage. We wire it in at Layer 1 with no
              threshold yet; Layer 12 turns it into a hard build gate.
              Adding it early means coverage is never a surprise.
            -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals><goal>prepare-agent</goal></goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>verify</phase>
                        <goals><goal>report</goal></goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
